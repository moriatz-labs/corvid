# exec.md

## Purpose
Run Shelfmark locally so a product manager can request, verify, and review repository changes.

## Repositories
```yaml
repositories:
  - id: shelfmark
    repo: moriatz-labs/shelfmark
    role: Public bookmarking product for PM judge requests.
    install: npm install
    dev: npm run dev -- --host 0.0.0.0
    health: http://localhost:5175
```

## Environment
```yaml
global:
  - name: VITE_CLERK_PUBLISHABLE_KEY
    required: false
    description: Clerk publishable key for authenticated product access.
  - name: VITE_SUPABASE_URL
    required: false
    description: Supabase browser credential for persisted product data.
  - name: VITE_SUPABASE_ANON_KEY
    required: false
    description: Supabase browser credential for persisted product data.
  - name: VITE_NOVUS_PENDO_API_KEY
    required: false
    description: Novus/Pendo analytics key for product usage events.
perRepo: {}
```

## Local Run Notes
Generated by Corvin AI onboarding from moriatz-labs/shelfmark on main. Verify with npm test and npm run build. Capture PM evidence from /, /collections, /search. Novus/Pendo analytics is expected to track login, bookmark creation, collection filtering, search, and tag use. Environment values can be added in Vercel or local .env files after onboarding; the packet remains valid without exposing secrets.
